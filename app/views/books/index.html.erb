<h1>Books</h1>

<table>
	<tr>
		<th></th>
		<th>Title</th>
		<th>Opinion</th>
	</tr>

	<tbody>
		<% @books.each do |book| %>
		<tr>
			<td><%= "" %></td>
			<td><%= book.title %></td>
			<td><%= book.body %></td>
		</tr>
		<% end %>
	</tbody>
</table>


<%= render 'users/profile', { user: @user, book: @book } %>